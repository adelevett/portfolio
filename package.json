{
  "name": "portfolio",
  "version": "1.0.0",
  "description": "Professional development travel log",
  "scripts": {
    "build": "eleventy",
    "start": "eleventy --serve",
    "generate": "node src/generate.mjs",
    "generate:sync-features": "node src/generate.mjs --sync-features",
    "test:vl": "node src/test-vl.mjs"
  },
  "dependencies": {
    "dotenv": "^16.0.0",
    "openai": "^4.0.0"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0"
  }
}
